/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    // Si vous avez besoin d'un basePath, décommentez la ligne suivante
    // basePath: '/pizza-app-next_js',
  }
  
  export default nextConfig;